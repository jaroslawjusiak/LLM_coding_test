# Fix the security issues

Fix every security issue in this application and add tests where practical.

Cover SQL injection, path traversal, admin authorization, password logging, and the React comment renderer. Do not change the happy-path behavior. Run `dotnet test` and `npm test`.

The project to edit is this workspace. Do not assume any files exist outside it.

When you finish, write `ANSWER.md` at the workspace root. State the root cause, what you changed, and what you deliberately left alone.

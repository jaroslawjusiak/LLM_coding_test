# Make the orders client match the contract

docs/orders-api.openapi.yaml is the contract for POST /api/orders.

The React client does not match it. Update the client so the tests pass. Do not change the OpenAPI file or the tests.

Run `npm test`.

The project to edit is this workspace. Do not assume any files exist outside it.

When you finish, write `ANSWER.md` at the workspace root. State the root cause, what you changed, and what you deliberately left alone.

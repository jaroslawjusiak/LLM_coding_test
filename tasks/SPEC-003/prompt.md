# Where is account deletion authorized?

Where is the authorization decision for deleting an account made? Explain the call chain.

Do not modify the code. Similar type names, a generated client, and a legacy class are not necessarily the decision. Write the call chain and the rule in ANSWER.md.

The project to edit is this workspace. Do not assume any files exist outside it.

When you finish, write `ANSWER.md` at the workspace root. State the root cause, what you changed, and what you deliberately left alone.
